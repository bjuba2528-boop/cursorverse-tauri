# 🐾 DPET - Desktop Pet Engine

## 🎉 Полнофункциональный движок для анимированных питомцев

**DPET (Desktop Pet Engine)** - это мощный движок для создания анимированных питомцев на рабочем столе, вдохновленный проектом [Deppo от SpikeHD](https://github.com/SpikeHD/Deppo).

---

## ✨ Возможности

### Основной функционал
- ✅ **Система анимаций** - Поддержка GIF анимаций для разных состояний
- ✅ **Физика** - Гравитация, трение, столкновения с границами экрана
- ✅ **Поведение** - Автономное движение, случайная ходьба
- ✅ **Перетаскивание** - Drag & drop с инерцией
- ✅ **Клик-взаимодействие** - Реакция на клики мышью
- ✅ **Импорт пакетов** - Поддержка .dpet (ZIP) и папок
- ✅ **Управление** - Создание, удаление, мониторинг питомцев
- ✅ **Сохранение состояния** - Автоматическое восстановление при запуске

### Состояния питомца
1. **Idle** (Покой) - Питомец стоит на месте
2. **Walk** (Ходьба) - Перемещение по экрану
3. **Falling** (Падение) - Падение под действием гравитации
4. **Drag** (Перетаскивание) - Пользователь тащит питомца
5. **Click** (Клик) - Реакция на клик мышью

---

## 📦 Формат DPET пакета

### Структура папки

```
my-pet/
├── dpet.json          # Конфигурация
└── animations/
    ├── idle/
    │   └── idle_1.gif
    ├── walk/
    │   └── walk_1.gif
    ├── fall/
    │   └── fall_1.gif
    ├── drag/
    │   └── drag_1.gif
    └── click/
        └── click_1.gif
```

### Конфигурация dpet.json

```json
{
  "name": "Мой питомец",
  "author": "Ваше имя",
  "fps": 30,
  "scale": 1.0,
  "behavior_change_rarity": 40.0,
  
  "can_move": true,
  "can_drag": true,
  "can_click": true,
  "can_fall": true,
  
  "move_speed": 2.0,
  
  "physics": {
    "max_velocity": 40.0,
    "friction": 0.9,
    "gravity": 2.0
  },
  
  "animations": {
    "idle": ["animations/idle/idle_1.gif"],
    "walk": ["animations/walk/walk_1.gif"],
    "fall": ["animations/fall/fall_1.gif"],
    "drag": ["animations/drag/drag_1.gif"],
    "click": ["animations/click/click_1.gif"]
  }
}
```

### Параметры конфигурации

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `name` | string | - | Название питомца |
| `author` | string | null | Автор питомца |
| `fps` | number | 30 | Частота кадров анимации |
| `scale` | number | 1.0 | Масштаб питомца |
| `behavior_change_rarity` | number | 40.0 | Редкость смены поведения (чем больше, тем реже) |
| `can_move` | boolean | true | Может ли питомец двигаться автономно |
| `can_drag` | boolean | true | Можно ли перетаскивать мышью |
| `can_click` | boolean | true | Реагирует ли на клики |
| `can_fall` | boolean | true | Применяется ли гравитация |
| `move_speed` | number | 2.0 | Скорость движения |
| `physics.max_velocity` | number | 40.0 | Максимальная скорость |
| `physics.friction` | number | 0.9 | Коэффициент трения (0-1) |
| `physics.gravity` | number | 2.0 | Сила гравитации |

---

## 🚀 Использование

### Импорт пакета

1. Откройте вкладку **🐾 DPET** в CursorVerse
2. Нажмите **"➕ Импортировать пакет"**
3. Выберите папку с DPET пакетом или .dpet/.zip файл
4. Пакет появится в списке доступных

### Создание питомца

1. Выберите пакет из списка
2. Нажмите **"✨ Создать питомца"**
3. Питомец появится на рабочем столе!

### Взаимодействие

- **Перетаскивание**: Зажмите левую кнопку мыши и тащите
- **Бросок с инерцией**: Отпустите при движении мыши
- **Клик**: Нажмите на питомца для спецэффекта

### Удаление

1. Вернитесь в DPET Manager
2. Найдите питомца в списке "Активные питомцы"
3. Нажмите **"🗑️ Удалить"**

---

## 🛠️ Создание собственного пакета

### Шаг 1: Подготовка анимаций

Создайте GIF анимации для каждого состояния:
- Рекомендуемый размер: 128x128 - 256x256 пикселей
- Формат: GIF с прозрачным фоном
- Длительность: 0.5-2 секунды

### Шаг 2: Структура папок

```bash
my-awesome-pet/
├── dpet.json
└── animations/
    ├── idle/
    │   ├── idle_1.gif
    │   └── idle_2.gif    # Можно несколько вариантов!
    ├── walk/
    │   └── walk_1.gif
    ├── fall/
    │   └── fall_1.gif
    ├── drag/
    │   └── drag_1.gif
    └── click/
        └── click_1.gif
```

### Шаг 3: Конфигурация

Создайте `dpet.json` с настройками (см. пример выше).

### Шаг 4: Упаковка (опционально)

```bash
# Создайте ZIP архив
zip -r my-pet.dpet my-awesome-pet/

# Или используйте 7-Zip в Windows
7z a my-pet.dpet my-awesome-pet/
```

### Шаг 5: Импорт

Импортируйте папку или .dpet файл через DPET Manager!

---

## 🎨 Примеры идей для питомцев

1. **Кот-ниндзя** 🐱
   - Idle: Сидит и облизывается
   - Walk: Крадется
   - Fall: Кувыркается
   - Click: Атакует лапой

2. **Летающий дракон** 🐉
   - Idle: Парит на месте
   - Walk: Летит горизонтально
   - Fall: Пикирует
   - Click: Выдыхает огонь

3. **Робот-помощник** 🤖
   - Idle: Мигает индикаторами
   - Walk: Катится на колесах
   - Fall: Летит на ракете
   - Click: Открывает панель

4. **Призрак** 👻
   - Idle: Медленно колышется
   - Walk: Плывет по воздуху
   - Fall: Пикирует вниз
   - Click: Пугает (boo!)

---

## 🔧 API для разработчиков

### Tauri Commands

```typescript
// Загрузить все пакеты
await invoke('dpet_load_packages') → DPetPackageInfo[]

// Импортировать пакет
await invoke('dpet_import_package', { path: string }) → string (package_id)

// Создать питомца
await invoke('dpet_create_pet', { packageId: string }) → string (pet_id)

// Удалить питомца
await invoke('dpet_remove_pet', { petId: string })

// Получить всех активных питомцев
await invoke('dpet_get_all_pets') → DPet[]

// Получить все пакеты
await invoke('dpet_get_all_packages') → DPetPackageInfo[]

// Обновить позицию питомца
await invoke('dpet_update_position', { petId: string, x: number, y: number })

// Обновить состояние питомца
await invoke('dpet_update_state', { petId: string, state: MovementState })

// Получить данные питомца
await invoke('dpet_get_pet_data', { petId: string }) → DPet | null

// Восстановить питомцев после запуска
await invoke('dpet_restore_pets')
```

### Типы данных

```typescript
interface DPetPackageInfo {
  id: string
  name: string
  author?: string
  path: string
  preview_image?: string // Base64
}

interface DPet {
  id: string
  config: DPetConfig
  package_path: string
  state: MovementState
  position: [number, number]
  velocity: [number, number]
  current_frame: number
  flip_x: boolean
  flip_y: boolean
  width: number
  height: number
}

type MovementState = 'Idle' | 'Walk' | 'Drag' | 'Falling' | 'Click'
```

---

## 📊 Производительность

### Ресурсы на одного питомца
- **Память**: 15-25 МБ
- **CPU (покой)**: <1%
- **CPU (активность)**: 3-8%

### Рекомендации
- **Оптимально**: 3-5 питомцев
- **Максимум**: 10-15 питомцев
- **Размер GIF**: Не более 500 КБ на файл
- **FPS**: 24-30 для плавности

---

## 🔍 Отладка

### Режим отладки

Добавьте в `dpet.json`:
```json
{
  "show_debug_info": true
}
```

Это покажет информацию в углу окна питомца:
- Текущее состояние
- Номер кадра
- FPS
- Скорость (velocity)

### Логи

Проверьте консоль DevTools для сообщений:
```javascript
// В dpet.html
console.log('Loaded pet:', petData)
console.log('Switching to state:', newState)
```

---

## 🎓 Продвинутые техники

### Множественные варианты анимаций

```json
{
  "animations": {
    "idle": [
      "animations/idle/idle_1.gif",
      "animations/idle/idle_2.gif",
      "animations/idle/idle_3.gif"
    ]
  }
}
```

Система выберет случайную анимацию при переходе в состояние.

### Отключение физики

```json
{
  "can_fall": false,
  "can_move": false
}
```

Создает статичного питомца, которого можно только перетаскивать.

### Настройка поведения

```json
{
  "behavior_change_rarity": 100.0,  // Редко меняет поведение
  "move_speed": 5.0,                // Быстрое движение
  "physics": {
    "friction": 0.95,               // Сильное трение
    "gravity": 1.0                  // Слабая гравитация
  }
}
```

---

## 🐛 Известные проблемы

1. **GIF не загружается**: Проверьте путь в `animations`
2. **Питомец проваливается**: Увеличьте `gravity` или `friction`
3. **Слишком быстро двигается**: Уменьшите `move_speed` или `max_velocity`
4. **Не восстанавливается**: Проверьте файл `pets_state.json` в `%APPDATA%/com.cursorverse.dev/dpets/`

---

## 📚 Ресурсы

### Где найти GIF анимации
- **GIPHY** - giphy.com
- **Tenor** - tenor.com
- **Sprite databases** - spriters-resource.com

### Инструменты для создания GIF
- **Aseprite** - Пиксель-арт анимации
- **GIMP** - Редактирование GIF
- **Photoshop** - Профессиональная анимация
- **ezgif.com** - Онлайн редактор

### Вдохновение
- **Shimeji** - Классические desktop pets
- **Deppo** - github.com/SpikeHD/Deppo
- **Desktop Goose** - Вирусный мем-питомец
- **eSheep** - Windows 95 овечка

---

## 🤝 Вклад в проект

### Создание пакетов
Создайте крутого питомца и поделитесь:
1. Запакуйте в .dpet
2. Создайте README с описанием
3. Поделитесь в Issues/Discussions

### Улучшение движка
- Новые состояния анимаций
- Улучшенная физика
- Звуковые эффекты
- Взаимодействие между питомцами

---

## 📝 История изменений

### v1.0.0 (Декабрь 2024)
- ✅ Первый релиз DPET Engine
- ✅ Поддержка GIF анимаций
- ✅ Базовая физика и поведение
- ✅ Импорт/экспорт пакетов
- ✅ UI для управления

---

## 💡 Планы на будущее

- [ ] Звуковые эффекты
- [ ] Переходные анимации между состояниями
- [ ] Взаимодействие между питомцами
- [ ] Система достижений
- [ ] Настройки расписания (появление/исчезновение)
- [ ] Интеграция с AI для "умного" поведения
- [ ] Поддержка видео (MP4)
- [ ] Маркетплейс пакетов

---

## ❤️ Благодарности

- **SpikeHD** - За вдохновение проектом [Deppo](https://github.com/SpikeHD/Deppo)
- **Tauri Team** - За amazing framework
- **Сообщество** - За идеи и фидбек

---

**Создано с 🐾 для CursorVerse**

**Версия**: 1.0.0  
**Дата**: Декабрь 2024  
**Лицензия**: MIT (следуйте лицензиям используемых GIF)
