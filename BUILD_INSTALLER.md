# 🔴 CursorVerse - Сборка установщика

## 📦 Быстрый старт

### 1. Запустите скрипт сборки:
```bash
build-installer.bat
```

Скрипт автоматически:
- ✅ Проверит зависимости (Node.js, Rust)
- ✅ Установит npm пакеты
- ✅ Соберет frontend
- ✅ Соберет Tauri приложение
- ✅ Создаст установщики (MSI + NSIS)
- ✅ Скопирует файлы в папку `release/`

### 2. Результаты сборки:

После успешной сборки в папке `release/` будут:

📁 **release/**
- `CursorVerse.exe` - Standalone приложение (без установки)
- `CursorVerse_1.5.0_x64_ru-RU.msi` - MSI установщик (Windows Installer)
- `CursorVerse_1.5.0_x64-setup.exe` - NSIS установщик (рекомендуется)

## 🎨 Стилизация установщика

### Crimson Theme элементы:
- ❤️ Красная цветовая схема (#DC143C - Crimson)
- 🤖 Иконка CursorVerse
- 🇷🇺 Русский язык по умолчанию
- ✨ Брендированные сообщения про Люси

### Что включает установщик:
1. **Приветственный экран** - с описанием возможностей
2. **Лицензионное соглашение** - CONDITIONS в стиле программы
3. **Выбор директории** - стандартно Program Files
4. **Установка** - с прогресс-баром
5. **Завершение** - с опцией запуска

### Автоматически создаются:
- ✅ Ярлык на рабочем столе
- ✅ Ярлыки в меню Пуск
- ✅ Запись в "Программы и компоненты"
- ✅ Автозапуск (опционально)

## 🛠 Ручная настройка

### Изменить иконку:
Замените файл `src-tauri/icons/CursorVerse.ico`

### Изменить цвета установщика:
Отредактируйте `src-tauri/installer.nsi`:
```nsi
!define MUI_BGCOLOR DC143C  ; Цвет фона (Crimson)
!define MUI_TEXTCOLOR FFFFFF ; Цвет текста (Белый)
```

### Изменить тексты:
В `src-tauri/installer.nsi` найдите:
```nsi
!define MUI_WELCOMEPAGE_TITLE "..."
!define MUI_WELCOMEPAGE_TEXT "..."
```

### Убрать автозапуск:
В `src-tauri/installer.nsi` закомментируйте:
```nsi
; WriteRegStr HKCU "Software\Microsoft\Windows\CurrentVersion\Run" "CursorVerse" "$INSTDIR\CursorVerse.exe --minimize"
```

## 📝 Требования для сборки

### Необходимо установлено:
1. **Node.js** (v18+) - https://nodejs.org
2. **Rust** (latest) - https://rustup.rs
3. **Visual Studio Build Tools** (для Windows)
4. **NSIS** (опционально, для кастомного установщика)

### Проверка:
```bash
node --version
npm --version
cargo --version
```

## 🚀 Сборка через npm:

### Только приложение (без установщика):
```bash
npm run tauri build
```

### Только frontend:
```bash
npm run build
```

### Dev режим:
```bash
npm run tauri dev
```

## 📤 Распространение

### MSI установщик:
- ✅ Поддержка корпоративного развертывания
- ✅ Тихая установка (`msiexec /i CursorVerse.msi /quiet`)
- ❌ Менее гибкая кастомизация

### NSIS установщик (рекомендуется):
- ✅ Полная кастомизация интерфейса
- ✅ Crimson тема
- ✅ Русский язык
- ✅ Меньший размер файла

### Portable версия:
Просто распространяйте `CursorVerse.exe` - не требует установки!

## 🔧 Устранение проблем

### "NSIS Error: Can't open..."
Установите NSIS: https://nsis.sourceforge.io/Download

### "Rust compilation failed"
```bash
rustup update
cargo clean
```

### "Node modules not found"
```bash
npm install
npm run build
```

## 📋 Чеклист перед релизом:

- [ ] Обновлена версия в `package.json`
- [ ] Обновлена версия в `src-tauri/tauri.conf.json`
- [ ] Обновлена версия в `src-tauri/Cargo.toml`
- [ ] Протестирована сборка
- [ ] Протестирована установка
- [ ] Протестирована удаление
- [ ] Создан changelog
- [ ] Обновлен `latest.json` для автообновлений

## 🎉 Готово!

После сборки у вас будет профессиональный установщик в стиле CursorVerse Crimson Edition с AI-помощником Люси!

---

**CursorVerse Team** | Made with ❤️ and Gemini AI
