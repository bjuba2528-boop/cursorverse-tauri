# 🔊 TTS (Озвучка) для Lucy AI

## Что добавлено

Lucy теперь может **говорить**! Все её ответы озвучиваются женским голосом через встроенный Web Speech API браузера.

---

## 🎤 Функции

- ✅ **Автоматическая озвучка** всех ответов Lucy
- ✅ **Женский голос** с оптимальными параметрами (pitch 1.1)
- ✅ **Поддержка 3 языков:** Русский, Украинский, Английский
- ✅ **Управление:** Включение/выключение кнопкой 🔊/🔇
- ✅ **Чистка текста:** Автоматически удаляет команды `[EXECUTE:...]` перед озвучкой
- ✅ **Сохранение настроек** в localStorage

---

## 🎛️ Использование

### Включение/выключение TTS

В интерфейсе Lucy AI есть кнопка **🔊 TTS** (рядом с настройками LLM):

- **🔊 TTS** - озвучка включена (зелёная)
- **🔇 TTS** - озвучка выключена (серая)

Настройка сохраняется между перезапусками приложения.

### Поддерживаемые языки

TTS автоматически переключается в зависимости от языка интерфейса:

- **Русский** → `ru-RU`
- **Українська** → `uk-UA`
- **English** → `en-US`

---

## 🗣️ Доступные голоса

Web Speech API использует системные голоса Windows. По умолчанию:

### Windows 10/11 (русский):
- **Microsoft Irina** - женский голос (по умолчанию)
- **Microsoft Pavel** - мужской голос

### Установка дополнительных голосов:

1. **Настройки Windows** → **Время и язык** → **Речь**
2. Нажмите **Управление голосами**
3. Нажмите **Добавить голоса**
4. Выберите нужный язык и голос
5. Нажмите **Добавить**

**Рекомендуемые голоса:**

- **Microsoft Irina Desktop (ru-RU)** ⭐ Уже установлен
- **Microsoft Svetlana (ru-RU)** - альтернативный женский
- **Microsoft Helena (uk-UA)** - украинский женский
- **Microsoft Zira (en-US)** - английский женский

---

## ⚙️ Параметры озвучки

Текущие настройки (в файле `ttsService.ts`):

```typescript
await ttsService.speak(text, {
  lang: 'ru-RU',     // Язык
  rate: 1.0,         // Скорость (0.1 - 10)
  pitch: 1.1,        // Тон (0 - 2) - выше для женского голоса
  volume: 0.8        // Громкость (0 - 1)
})
```

### Настройка параметров:

Отредактируйте `src/components/LucyAssistant.tsx`, функцию `speakResponse`:

- **Медленнее:** `rate: 0.8`
- **Быстрее:** `rate: 1.3`
- **Ниже тон:** `pitch: 0.9`
- **Выше тон:** `pitch: 1.3`
- **Громче:** `volume: 1.0`
- **Тише:** `volume: 0.5`

---

## 🔧 Технические детали

### Архитектура

```
LucyAssistant.tsx
      ↓
  speakResponse(text)
      ↓
  ttsService.speak(text, options)
      ↓
  Web Speech API
      ↓
  🔊 Системный голос
```

### Фильтрация команд

Перед озвучкой текст очищается от команд:

```typescript
const cleanText = text.replace(/\[EXECUTE:.+?\]/g, '').trim()
```

**Пример:**
- **До:** "Открываю блокнот [EXECUTE:APP:notepad]"
- **После:** "Открываю блокнот"

### Выбор голоса

Приоритет выбора женского голоса:

1. Ищет голос с ключевыми словами: `female`, `woman`, `anna`, `elena`, `irina`
2. Если не найден - любой русский голос
3. Если русского нет - голос с языковым кодом `ru`
4. В крайнем случае - первый доступный голос

---

## 🐛 Решение проблем

### ❌ "Озвучка не работает"

**Решение:**
1. Проверьте, что кнопка TTS включена (🔊 зелёная)
2. Убедитесь, что звук не выключен в Windows
3. Проверьте консоль браузера (F12) на ошибки
4. Перезагрузите приложение

### 🤐 "Нет женского голоса"

**Решение:**
1. Откройте **Настройки Windows** → **Речь**
2. Установите **Microsoft Irina** (должна быть по умолчанию)
3. Перезапустите приложение

### 🗣️ "Говорит мужским голосом"

**Решение:**
1. Откройте консоль (F12)
2. Посмотрите, какой голос используется:
   ```
   🗣️ Используется голос: Microsoft Pavel (ru-RU)
   ```
3. Установите женский голос через Настройки Windows
4. Или измените логику выбора в `ttsService.ts` → `getBestVoice()`

### ⏱️ "Медленная озвучка"

**Решение:**
Увеличьте `rate` в `LucyAssistant.tsx`:
```typescript
await ttsService.speak(cleanText, {
  rate: 1.3,  // Быстрее
  ...
})
```

### 🔇 "Озвучка обрывается"

**Решение:**
1. Проверьте, что Lucy не получает новые команды во время озвучки
2. Убедитесь, что браузер не переходит в фоновый режим
3. Увеличьте таймаут в `ttsService.ts` (если нужно)

---

## 🎨 Расширенная настройка

### Добавить свой голос

Если у вас установлен сторонний TTS движок (например, SAPI5), он появится в списке голосов.

Чтобы явно использовать конкретный голос:

```typescript
// В ttsService.ts → speak()
const voice = this.voices.find(v => v.name === 'Microsoft Irina Desktop - Russian')
if (voice) {
  utterance.voice = voice
}
```

### Использовать Coqui TTS (Python)

Для более качественной озвучки можно интегрировать **Coqui TTS** через Python backend:

1. Установите Coqui TTS:
   ```bash
   pip install TTS
   ```

2. Создайте Python сервер:
   ```python
   from TTS.api import TTS
   tts = TTS("tts_models/ru/ruslan/vits")
   tts.tts_to_file(text="Привет!", file_path="output.wav")
   ```

3. Вызывайте через Tauri команды из фронтенда

---

## 📊 Производительность

### Web Speech API:
- ✅ **Задержка:** ~100-200ms (быстро)
- ✅ **Качество:** Среднее (системные голоса)
- ✅ **Ресурсы:** Минимальные (CPU ~5%)
- ✅ **Поддержка:** Встроена в браузер

### Coqui TTS (альтернатива):
- ⚠️ **Задержка:** ~1-3 секунды (медленнее)
- ✅ **Качество:** Отличное (нейронные голоса)
- ⚠️ **Ресурсы:** Высокие (CPU 30%+, GPU рекомендуется)
- ⚠️ **Установка:** Требует Python + зависимости

---

## 📝 API методы

### `ttsService.speak(text, options)`
Озвучить текст с параметрами.

### `ttsService.stop()`
Остановить текущую озвучку.

### `ttsService.pause()`
Поставить на паузу.

### `ttsService.resume()`
Продолжить озвучку.

### `ttsService.setEnabled(boolean)`
Включить/выключить TTS.

### `ttsService.isEnabled()`
Проверить статус TTS.

### `ttsService.isSpeaking()`
Проверить, идёт ли озвучка.

### `ttsService.getAvailableVoices()`
Получить список доступных голосов.

---

## 🌟 Будущие улучшения

- [ ] Выбор голоса из интерфейса
- [ ] Регулятор скорости/тона
- [ ] Поддержка SSML (эмоции, паузы)
- [ ] Кэширование озвучки
- [ ] Интеграция с Coqui TTS
- [ ] Голосовые эффекты (эхо, реверберация)

---

**Готово!** Lucy теперь может говорить! 🔊✨
