# 🎤 Voice Lucy Assistant - Complete Integration Guide

## ✨ Что нового

Voice Lucy Assistant полностью интегрирован в CursorVerse Tauri с расширенными возможностями голосового управления!

### 🎯 Ключевые функции

#### 🎙️ Голосовое распознавание
- **Распознавание речи на русском языке** через Web Speech API
- **Визуальная обратная связь** с пульсирующей анимацией
- **Автоматическая обработка** распознанного текста
- **Индикаторы уверенности** для каждого распознавания
- **Обработка ошибок** микрофона и API

#### 👏 Детекция хлопков
- **Hands-free активация** - хлопните один раз для запуска распознавания
- **Интеллектуальная детекция** с регулируемой чувствительностью
- **Фильтрация ложных срабатываний** с debounce
- **Визуальный feedback** при обнаружении хлопка

#### 🔊 Аудио процессинг
- **Реальное время анализ** входящего аудио
- **Детекция голосовой активности** (Voice Activity Detection)
- **Метрики качества аудио** (SNR, clipping detection)
- **Визуализация уровня** звука

#### 💬 Чат интерфейс
- **История сообщений** с автосохранением
- **Индикаторы печати** Люси
- **Экспорт истории** в JSON
- **Метаданные сообщений** (timestamp, confidence)

## 🚀 Быстрый старт

### Установка

```bash
# Клонируйте репозиторий
git clone https://github.com/bjuba2528-boop/cursorverse-tauri.git
cd cursorverse-tauri

# Установите зависимости
npm install

# Запустите в режиме разработки
npm run tauri dev
```

### Первое использование

1. **Разрешите доступ к микрофону** при первом запуске
2. **Нажмите кнопку микрофона (🎤)** или используйте `Ctrl+K`
3. **Говорите четко** на русском языке
4. **Дождитесь ответа Люси**

## ⌨️ Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `Ctrl+K` | Включить/выключить распознавание речи |
| `Ctrl+Shift+C` | Включить/выключить детекцию хлопков |
| `Space` | Toggle voice (в некоторых режимах) |
| `Escape` | Остановить распознавание |
| `F1` | Показать справку |
| `F11` | Информация о системе |
| `Ctrl+L` | Очистить чат |
| `Ctrl+H` | Показать статистику чата |

## 🎨 Интерфейс

### Кнопки управления

**🎤 Микрофон (красная)**
- Одиночный клик - запуск/остановка распознавания
- Пульсирует красным при прослушивании
- Горячая клавиша: `Ctrl+K`

**👏 Хлопки (фиолетовая)**
- Включение/выключение детекции хлопков
- Пульсирует фиолетовым когда активна
- Горячая клавиша: `Ctrl+Shift+C`

**➤ Отправить**
- Отправка текстового сообщения
- Активна только при наличии текста

## 📊 API и события

### Window API

```javascript
// Доступ к глобальному API
window.voiceLucy = {
    // Управление голосом
    startVoice: () => void,
    stopVoice: () => void,
    toggleVoice: () => void,
    
    // Управление хлопками
    startClap: () => void,
    stopClap: () => void,
    toggleClap: () => void,
    
    // Информация
    getStatus: () => string,
    isReady: () => boolean,
    getComponents: () => object,
    getSettings: () => object,
    
    // Утилиты
    showHelp: () => void,
    showInfo: () => void,
    notify: (message, type) => void,
    updateSettings: (settings) => void
}
```

### События

```javascript
// Слушаем события голосовой системы
window.addEventListener('voice-start', (event) => {
    console.log('Voice recognition started');
});

window.addEventListener('voice-end', (event) => {
    console.log('Voice recognition ended');
});

window.addEventListener('clap-detected', (event) => {
    console.log('Clap detected!', event.detail);
});

window.addEventListener('voice-recognized', (event) => {
    console.log('Text recognized:', event.detail.text);
});

window.addEventListener('audio-data-update', (event) => {
    console.log('Audio metrics:', event.detail);
});
```

## 🔧 Настройки

### Аудио процессор

```javascript
// Обновление настроек аудио
window.audioProcessor.updateSettings({
    sampleRate: 44100,
    bufferSize: 4096,
    noiseThreshold: 0.01,
    voiceActivityThreshold: 0.1,
    clapThreshold: 0.8,
    clapDebounce: 500
});
```

### Распознавание речи

```javascript
// Настройка распознавания
const recognition = window.voiceRecognition.recognition;
recognition.continuous = false;
recognition.interimResults = true;
recognition.lang = 'ru-RU';
recognition.maxAlternatives = 1;
```

### Детекция хлопков

```javascript
// Калибровка порога хлопков
window.clapDetection.calibrateClapThreshold();

// Установка порога вручную
window.clapDetection.setThreshold(0.7); // 0.1 - 0.9
```

## 🐛 Решение проблем

### Микрофон не работает

1. **Проверьте разрешения браузера**
   - Chrome: `chrome://settings/content/microphone`
   - Edge: `edge://settings/content/microphone`

2. **Используйте HTTPS или localhost**
   - Web Speech API требует защищенное соединение

3. **Проверьте консоль браузера** (F12)
   - Смотрите логи с префиксом 🎤

### Хлопки не детектируются

1. **Калибруйте чувствительность**
   ```javascript
   window.clapDetection.calibrateClapThreshold()
   ```

2. **Проверьте уровень аудио**
   - Смотрите индикатор уровня в интерфейсе
   - Должен быть > 80% при хлопке

3. **Отрегулируйте порог**
   ```javascript
   window.clapDetection.setThreshold(0.6) // Понизить для большей чувствительности
   ```

### Ошибки распознавания

- **"Speech recognition aborted"** - Игнорируется автоматически
- **"not-allowed"** - Разрешите доступ к микрофону
- **"no-speech"** - Говорите громче и четче
- **"network"** - Проверьте интернет соединение

## 📦 Архитектура компонентов

```
Voice Lucy Assistant
├── Audio Processor (audio-processor.js)
│   ├── Захват аудио с микрофона
│   ├── Анализ аудио метрик
│   ├── Детекция голосовой активности
│   └── Детекция хлопков
│
├── Voice Recognition (voice-recognition.js)
│   ├── Web Speech API интеграция
│   ├── Обработка распознанного текста
│   ├── Управление состоянием
│   └── Обработка ошибок
│
├── Clap Detection (clap-detection.js)
│   ├── Анализ паттернов хлопков
│   ├── Калибровка чувствительности
│   ├── Визуальная обратная связь
│   └── Автозапуск голоса
│
├── Chat Interface (chat-interface.js)
│   ├── Управление сообщениями
│   ├── История и сохранение
│   ├── UI обновления
│   └── Экспорт данных
│
└── Main Integration (main.js)
    ├── Координация компонентов
    ├── Глобальное API
    ├── Обработка событий
    └── Клавиатурные сокращения
```

## 🔗 Интеграция с React

### LucyAssistant.tsx

```tsx
// Подписка на события голосовой системы
useEffect(() => {
    const handleVoiceRecognized = (event: any) => {
        const text = event.detail?.text;
        if (text) {
            setInputText(text);
            handleSubmit(text);
        }
    };

    window.addEventListener('voice-recognized', handleVoiceRecognized);
    window.addEventListener('voice-start', () => setIsListening(true));
    window.addEventListener('voice-end', () => setIsListening(false));

    return () => {
        window.removeEventListener('voice-recognized', handleVoiceRecognized);
        // ... cleanup
    };
}, []);

// Toggle голоса
const toggleVoiceRecognition = () => {
    const voiceLucy = (window as any).voiceLucy;
    if (voiceLucy?.toggleVoice) {
        voiceLucy.toggleVoice();
    }
};
```

## 📱 Поддержка браузеров

| Браузер | Голос | Хлопки | Аудио | Статус |
|---------|-------|--------|-------|--------|
| Chrome/Chromium | ✅ | ✅ | ✅ | Полная поддержка |
| Microsoft Edge | ✅ | ✅ | ✅ | Полная поддержка |
| Safari | ⚠️ | ✅ | ✅ | Ограниченная поддержка голоса |
| Firefox | ❌ | ✅ | ✅ | Нет Web Speech API |

**Рекомендация:** Chrome или Edge для лучшего опыта

## 🎯 Примеры использования

### Простое голосовое управление

```javascript
// Запустить распознавание
window.voiceLucy.startVoice();

// Остановить
window.voiceLucy.stopVoice();

// Toggle
window.voiceLucy.toggleVoice();
```

### Получение статуса

```javascript
// Проверка готовности системы
if (window.voiceLucy.isReady()) {
    console.log('Voice Lucy готова!');
}

// Текущий статус
const status = window.voiceLucy.getStatus();
console.log('Status:', status); // 'ready', 'listening', 'processing', 'error'
```

### Отправка уведомлений

```javascript
// Информационное
window.voiceLucy.notify('Голос активирован', 'info');

// Успех
window.voiceLucy.notify('Хлопок обнаружен!', 'success');

// Ошибка
window.voiceLucy.notify('Микрофон не найден', 'error');

// Предупреждение
window.voiceLucy.notify('Низкая уверенность', 'warning');
```

### Настройка системы

```javascript
// Обновление настроек
window.voiceLucy.updateSettings({
    autoStart: true,
    clapEnabled: true,
    voiceEnabled: true,
    language: 'ru-RU'
});

// Получение текущих настроек
const settings = window.voiceLucy.getSettings();
console.log('Settings:', settings);
```

## 📚 Дополнительные ресурсы

- [Web Speech API Documentation](https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API)
- [Tauri Documentation](https://tauri.app/v2/)
- [React Documentation](https://react.dev/)

## 🤝 Вклад

Предложения и улучшения приветствуются! Создавайте Issues и Pull Requests на GitHub.

## 📄 Лицензия

MIT License - см. LICENSE файл

---

**🎉 Готово!** Voice Lucy Assistant полностью интегрирован и готов к использованию!

Нажмите `F1` в приложении для быстрой справки или `F11` для информации о системе.
