# 🏗️ AI Agent Architecture

## Системная Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     Frontend (React + TS)                    │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  AIAssistant.tsx                                             │
│  ├─ Wake Word Detection ("Люси")                            │
│  ├─ Voice Recognition (Web Speech API)                       │
│  └─ UI для чата с агентом                                    │
│                                                               │
│  LLMSettings.tsx                                             │
│  ├─ Настройка провайдера (OpenAI/LM Studio/Ollama/etc)      │
│  ├─ API ключи и конфигурация                                │
│  └─ Тест подключения                                         │
│                                                               │
├─────────────────────────────────────────────────────────────┤
│                     Execution Layer                          │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  autonomousAgent.ts                                          │
│  ├─ smartExecute(userCommand)                               │
│  │   ├─ Проверка llmService.isReady()                       │
│  │   ├─ Если готов → llmService.autonomousThink()           │
│  │   └─ Иначе → interpretAndExecute() (базовый режим)       │
│  │                                                            │
│  └─ Wrappers для Rust команд:                               │
│      ├─ executeShellCommand()                                │
│      ├─ openApplication()                                    │
│      ├─ createFile() / readFile()                            │
│      └─ getProcessList() / getSystemInfo()                   │
│                                                               │
├─────────────────────────────────────────────────────────────┤
│                   Universal LLM Service                      │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  llmService.ts (OpenAI-compatible)                           │
│  ├─ Провайдеры:                                              │
│  │   ├─ OpenAI (GPT-4, GPT-4o, GPT-4o-mini)                 │
│  │   ├─ LM Studio (локальные модели)                        │
│  │   ├─ Ollama (бесплатные модели)                          │
│  │   ├─ Anthropic (Claude)                                   │
│  │   └─ Custom (любой OpenAI-совместимый)                   │
│  │                                                            │
│  ├─ Методы:                                                  │
│  │   ├─ init() - подключение к провайдеру                   │
│  │   ├─ chat(messages) - обычный чат                        │
│  │   ├─ chatStream(messages) - стриминг                     │
│  │   └─ autonomousThink(request, maxIterations)             │
│  │                                                            │
│  └─ Autonomous Thinking Loop:                               │
│      ├─ 1. LLM анализирует запрос                            │
│      ├─ 2. Выбирает инструмент (TOOL)                        │
│      ├─ 3. Указывает параметры (PARAMS)                      │
│      ├─ 4. Объясняет причину (REASON)                        │
│      ├─ 5. Система выполняет → результат                     │
│      ├─ 6. LLM видит результат → следующий шаг               │
│      └─ 7. Повторять до DONE или 5 итераций                 │
│                                                               │
│  Доступные инструменты:                                      │
│  ├─ execute_command(command, args)                          │
│  ├─ open_application(app_name)                              │
│  ├─ create_file(path, content)                              │
│  ├─ read_file(path)                                          │
│  ├─ get_processes()                                          │
│  └─ get_system_info()                                        │
│                                                               │
├─────────────────────────────────────────────────────────────┤
│                  Backend (Rust + Tauri)                      │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  autonomous_agent.rs                                         │
│  ├─ execute_shell_command(command, args)                    │
│  │   └─ Command::new().args().output()                      │
│  │                                                            │
│  ├─ open_application(app_name)                              │
│  │   ├─ Windows: start app_name                             │
│  │   └─ Unix: open/xdg-open                                  │
│  │                                                            │
│  ├─ create_file(path, content)                              │
│  │   ├─ fs::create_dir_all() (создаёт папки)               │
│  │   └─ fs::write()                                          │
│  │                                                            │
│  ├─ read_file(path)                                          │
│  │   └─ fs::read_to_string()                                │
│  │                                                            │
│  ├─ get_process_list()                                       │
│  │   ├─ sysinfo::System                                     │
│  │   ├─ Сортировка по CPU+Memory                            │
│  │   └─ Топ 50 процессов                                    │
│  │                                                            │
│  ├─ get_system_info()                                        │
│  │   ├─ OS name, version, kernel                            │
│  │   ├─ CPU model, cores                                     │
│  │   ├─ Total/Used/Free Memory                              │
│  │   └─ Disks info                                           │
│  │                                                            │
│  ├─ kill_process(process_name)                              │
│  │   ├─ Windows: taskkill /F /IM                            │
│  │   └─ Unix: pkill -9                                       │
│  │                                                            │
│  ├─ execute_powershell(script)                              │
│  │   └─ powershell -Command "script"                        │
│  │                                                            │
│  └─ get_directory_contents(path)                            │
│      └─ fs::read_dir() → list files/folders                 │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Процесс Выполнения Команды

### 1. Голосовой Ввод
```
Пользователь → "Люси" → Wake Word Detection → Активация
              → "открой блокнот" → Speech Recognition → Текст
```

### 2. Обработка Команды
```
AIAssistant.tsx
  ↓
smartExecute(userCommand)
  ↓
if (llmService.isReady())
  ├─ ДА → Умный режим
  │   └─ llmService.autonomousThink(userCommand, 5)
  │
  └─ НЕТ → Базовый режим
      └─ interpretAndExecute(userCommand)
```

### 3. Autonomous Thinking (Умный Режим)

**Итерация 1:**
```
LLM получает:
  USER: "открой блокнот"
  SYSTEM: "У тебя есть инструменты: execute_command, open_application, ..."

LLM отвечает:
  TOOL: open_application
  PARAMS: {"app_name": "notepad"}
  REASON: Пользователь просит открыть блокнот, использую open_application

Система выполняет:
  invoke('open_application', {app_name: 'notepad'})
  → Windows: start notepad
  → Результат: "Приложение notepad успешно открыто"

LLM видит результат и отвечает:
  DONE: Блокнот успешно открыт!
```

**Сложный пример (несколько итераций):**
```
USER: "создай файл test.txt с текстом Hello и открой его"

Итерация 1:
  LLM → TOOL: create_file
  LLM → PARAMS: {"path": "test.txt", "content": "Hello"}
  Система → "Файл создан"

Итерация 2:
  LLM → TOOL: open_application
  LLM → PARAMS: {"app_name": "notepad test.txt"}
  Система → "Файл открыт в блокноте"

Итерация 3:
  LLM → DONE: Файл создан и открыт!
```

### 4. Базовый Режим (без LLM)

```javascript
interpretAndExecute(userCommand) {
  if (command.includes('открой')) {
    // Извлекаем название приложения
    const app = extractAppName(command)
    return openApplication(app)
  }
  else if (command.includes('процесс')) {
    return getProcessList()
  }
  // ... другие правила
}
```

---

## 📊 Поддерживаемые Провайдеры

| Провайдер | Endpoint | Auth | Streaming | Cost |
|-----------|----------|------|-----------|------|
| OpenAI | `api.openai.com/v1` | `Bearer sk-...` | ✅ | $0.15-10/1M |
| LM Studio | `localhost:1234/v1` | - | ✅ | FREE |
| Ollama | `localhost:11434/v1` | - | ✅ | FREE |
| Anthropic | `api.anthropic.com/v1` | `x-api-key: sk-ant-...` | ✅ | $3/1M |
| Custom | Любой | Любой | ✅ | Зависит |

---

## 🎯 Примеры Использования

### Простая команда
```typescript
// Голос: "Люси, открой калькулятор"

smartExecute("открой калькулятор")
  → llmService.autonomousThink(...)
    → LLM выбирает: open_application("calc")
      → Rust: start calc
        → Результат: "Калькулятор открыт"
```

### Сложная команда
```typescript
// Голос: "Люси, покажи 5 процессов с самой большой памятью"

smartExecute("покажи 5 процессов с самой большой памятью")
  → llmService.autonomousThink(...)
    
    Итерация 1:
    → LLM: "Использую get_processes для получения списка"
    → Rust: get_process_list() → JSON со всеми процессами
    
    Итерация 2:
    → LLM: "Сортирую по памяти и беру топ-5"
    → LLM: "DONE: [список процессов с форматированием]"
```

### Команда с созданием файла
```typescript
// Голос: "Люси, создай файл readme.md с текстом Hello World"

smartExecute("создай файл readme.md с текстом Hello World")
  → llmService.autonomousThink(...)
    
    Итерация 1:
    → LLM: "TOOL: create_file"
    → LLM: "PARAMS: {path: 'readme.md', content: 'Hello World'}"
    → Rust: fs::write("readme.md", "Hello World")
    → Результат: "Файл создан"
    
    Итерация 2:
    → LLM: "DONE: Файл readme.md успешно создан с текстом Hello World"
```

---

## 🔒 Безопасность

### Ограничения
- Нет удаления файлов по умолчанию
- Нет доступа к системным папкам (можно добавить)
- Максимум 5 итераций (защита от зацикливания)
- Нет выполнения опасных команд без подтверждения

### Хранение Данных
- API ключи: `localStorage` (только браузер)
- История команд: в памяти (не сохраняется)
- Кастомные команды: `localStorage`

---

## 🚀 Производительность

| Метод | Скорость | Ресурсы | Приватность |
|-------|----------|---------|-------------|
| OpenAI API | ⚡⚡⚡ Быстро | 🌐 Интернет | ❌ Облако |
| LM Studio | ⚡⚡ Средне | 💻 8GB+ RAM | ✅ Локально |
| Ollama | ⚡ Медленно | 💻 4GB+ RAM | ✅ Локально |
| Базовый режим | ⚡⚡⚡ Мгновенно | - | ✅ Оффлайн |

---

## 📝 Настройка

Все настройки через UI (кнопка ⚙️):
```typescript
interface LLMConfig {
  provider: 'openai' | 'lmstudio' | 'ollama' | 'anthropic' | 'local'
  apiKey?: string           // Для OpenAI/Anthropic
  baseURL?: string          // URL сервера
  model: string             // Название модели
  temperature: number       // 0-1 (креативность)
  maxTokens: number         // Длина ответа
}
```

---

## 🎓 Расширение

Добавить новый инструмент:

1. **Rust** (`autonomous_agent.rs`):
```rust
#[tauri::command]
fn my_new_tool(param: String) -> Result<String, String> {
    // Ваш код
    Ok("Результат".to_string())
}
```

2. **TypeScript** (`llmService.ts`):
```typescript
{
  name: 'my_new_tool',
  description: 'Описание',
  execute: async (params) => {
    return await invoke('my_new_tool', { param: params.param })
  }
}
```

3. **Регистрация** (`main.rs`):
```rust
.invoke_handler(tauri::generate_handler![
  // ...
  autonomous_agent::my_new_tool
])
```

---

**Архитектура готова к расширению и масштабированию! 🚀**
