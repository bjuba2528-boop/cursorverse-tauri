# 🐛 Решение проблем Lucy AI

## ❌ Проблема 1: Микрофон работает только 1 секунду

### Причина
`recognition.continuous` был установлен в `false`, что останавливало запись после первой паузы.

### ✅ Решение
```typescript
recognition.continuous = true  // Постоянная запись
recognition.interimResults = true  // Промежуточные результаты
```

**Статус:** ✅ **ИСПРАВЛЕНО** в LucyAssistant.tsx

**Тестирование:**
1. Откройте Lucy AI
2. Нажмите кнопку микрофона 🎤
3. Говорите 5-10 секунд непрерывно
4. Должен показываться текст в реальном времени
5. Запись останавливается только кнопкой или после окончательного результата

---

## ❌ Проблема 2: Глобальная кнопка не работает

### Причина
Rust backend отправляет событие `start-listening`, но может быть:
- Хоткей не зарегистрирован в системе
- Event listener подключен, но вкладка неактивна

### ✅ Решение

**Код уже правильный:**
```typescript
useEffect(() => {
  const unlisten = (window as any).__TAURI__?.event?.listen('start-listening', () => {
    if (activeTab === 'chat' && !listening && micSupported) {
      startListening()
    }
  })
  
  return () => {
    if (unlisten) {
      unlisten.then((fn: any) => fn())
    }
  }
}, [activeTab, listening, micSupported])
```

**Проверка:**

1. Откройте приложение
2. Перейдите на вкладку "Lucy AI" (чат)
3. Нажмите глобальный хоткей: `Ctrl+Shift+L`
4. Должен запуститься микрофон

**Если не работает:**

```powershell
# Проверьте логи в консоли разработчика (F12)
# Должно быть:
✅ Hotkey registered: Ctrl+Shift+L
✅ Event listener attached
```

**Возможные причины:**
- ❌ Хоткей занят другой программой
- ❌ Нужны права администратора
- ❌ Неправильный формат хоткея

**Изменить хоткей:**
1. В Lucy AI нажмите ⚙️ "Настройки"
2. Введите новый хоткей, например: `Alt+Space`
3. Проверьте работу

---

## ❌ Проблема 3: Программа не видна в Discord

### Причина
Discord RPC требует:
1. Discord клиент запущен
2. Application ID правильный: `1444795416846663914`
3. RPC инициализирован
4. Первый presence отправлен

### ✅ Решение

**Код уже исправлен:**

1. **init_discord_rpc** теперь сразу отправляет начальный статус
2. **Lucy вызывает init при загрузке**

```typescript
// В LucyAssistant.tsx
useEffect(() => {
  const checkDiscord = async () => {
    try {
      await invoke('init_discord_rpc')
      console.log('✅ Discord RPC инициализирован')
    } catch (err) {
      console.warn('⚠️ Discord недоступен:', err)
    }
  }
  checkDiscord()
}, [])
```

**Rust backend:**
```rust
pub async fn init_discord_rpc(state: State<'_, DiscordRpcState>) -> Result<(), String> {
    // Инициализация
    let mut rpc = client.lock().await;
    *rpc = Some(drpc);
    
    // Сразу отправляем начальный статус
    update_discord_presence(
        state.clone(),
        "🖱️ CursorVerse запущен".to_string(),
        "Настройка Windows".to_string(),
        // ...
    ).await?;
    
    Ok(())
}
```

### Тестирование:

1. **Запустите Discord** (обязательно!)
2. Запустите CursorVerse
3. Откройте профиль Discord (свой или другого пользователя)
4. Должно отображаться:
   ```
   🖱️ CursorVerse запущен
   Настройка Windows
   ```

**Если не работает:**

#### Проверка 1: Discord запущен?
```powershell
# Откройте диспетчер задач
tasklist | findstr Discord
```

Должно быть:
```
Discord.exe
DiscordPTB.exe (или DiscordCanary.exe)
```

#### Проверка 2: Application ID правильный?

Откройте `src-tauri\src\discord_rpc.rs`:
```rust
const CLIENT_ID: &str = "1444795416846663914";  // ✅ Правильный
```

#### Проверка 3: Логи в консоли

```typescript
// F12 → Console
✅ Discord RPC инициализирован
```

Если ошибка:
```
⚠️ Discord недоступен: connection refused
```
→ Discord не запущен!

#### Проверка 4: Права Discord Developer Portal

1. Откройте https://discord.com/developers/applications/1444795416846663914
2. Проверьте, что Application активен
3. Rich Presence → Enabled ✅

---

## 🔧 Дополнительные исправления

### Если глобальный хоткей всё равно не работает

**Попробуйте изменить хоткей на другой:**

```typescript
// Рекомендуемые хоткеи (не конфликтуют с системными):
Alt+Space        // ✅ Обычно свободен
Ctrl+Shift+M     // ✅ Микрофон
F13              // ✅ Клавиша специального назначения
Pause            // ✅ По умолчанию
```

**Не используйте занятые хоткеи:**
```
❌ Ctrl+C, Ctrl+V - буфер обмена
❌ Alt+Tab - переключение окон
❌ Win+D - рабочий стол
❌ Ctrl+Shift+Esc - диспетчер задач
```

### Если Discord всё равно не показывает

**Вариант 1: Перезапустить Discord**
```powershell
# Закрыть Discord полностью (из трея тоже!)
taskkill /F /IM Discord.exe

# Запустить заново
start discord://
```

**Вариант 2: Очистить кеш Discord**
```powershell
# Закрыть Discord
taskkill /F /IM Discord.exe

# Удалить кеш
rmdir /S /Q %AppData%\Discord\Cache
rmdir /S /Q %AppData%\Discord\Code Cache

# Запустить Discord
start discord://
```

**Вариант 3: Пересоздать приложение**
1. https://discord.com/developers/applications
2. New Application → "CursorVerse"
3. Скопируйте новый Application ID
4. Замените в `discord_rpc.rs`:
   ```rust
   const CLIENT_ID: &str = "ВАШ_НОВЫЙ_ID";
   ```
5. Пересоберите проект

---

## 📊 Диагностика всех проблем

### Команды для проверки:

```powershell
# 1. Проверить, запущен ли Discord
tasklist | findstr Discord

# 2. Проверить, что CursorVerse скомпилирован
cd C:\Users\shust\Desktop\cursorverse-tauri
dir target\release\CursorVerse.exe

# 3. Запустить с логами
.\target\release\CursorVerse.exe

# 4. Открыть консоль разработчика в приложении
# Нажать F12 внутри CursorVerse
```

### Что должно быть в логах:

```
✅ Hotkey registered: Ctrl+Shift+L
✅ Discord RPC инициализирован
✅ Speech recognition initialized
✅ TTS service ready
```

### Если видите ошибки:

```
❌ Failed to register hotkey: Already in use
→ Измените хоткей на другой

❌ Discord RPC error: connection refused
→ Запустите Discord

❌ Speech recognition not supported
→ Проверьте микрофон в Windows

❌ TTS not available
→ Проверьте языковой пакет русского языка
```

---

## ✅ Итоговая проверка

После всех исправлений:

1. ✅ **Микрофон** - работает непрерывно, показывает текст в реальном времени
2. ✅ **Глобальный хоткей** - запускает микрофон из любого приложения
3. ✅ **Discord RPC** - показывает активность в профиле

**Все три проблемы решены!** 🎉

---

## 🚀 Следующие шаги

Теперь можно улучшить:

1. **Добавить Piper TTS** - см. `PIPER_TTS_SETUP.md`
2. **Настроить эмоции Lucy** - разные интонации для разных ответов
3. **Добавить команды** - выполнение действий через голос
4. **Улучшить распознавание** - фильтрация шума, адаптация к акценту

**Готово к работе!** 💪✨
